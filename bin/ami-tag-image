#!/usr/bin/env python

from amitools import clsetup
clsetup()

if '__main__' == __name__:
    from amitools.tools.ami_tag_image import (
        get_args,
        main,
        )
    args = get_args()
    result = main(args)
    
